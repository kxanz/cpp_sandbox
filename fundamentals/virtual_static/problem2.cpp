#include <iostream>

/*
 * Virtual Functions and Polymorphism
 * 
 * Virtual function 
 * - is a special type function 
 * - when called it resolved the most derived version of the function 
 *   for the acual type of the object being referenced or pointed to
 * - A derived function is considered a match if it has the same
 *   signiture (name, parameter types, and if its a const) such
 *   fucntions are called OVERRIDES
 *
 * Polymorphism
 * - the ability for an entity to take many forms 
 *
 * Compile Time polymorphisms 
 * - refers to forms of polymorphisms that are resolved by compile Time
 *
 * Runtime Polymorphism
 * - resolved at runtimem this includes virtual function resolution
 */


int main() {

    return 0;
}
